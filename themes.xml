<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.NewBewell" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.NewBewell" parent="Base.Theme.NewBewell" />
    
    <!-- Modern Switch Style -->
    <style name="ModernSwitch" parent="Widget.Material3.CompoundButton.Switch">
        <item name="thumbTint">@color/primary_green</item>
        <item name="trackTint">@color/surface_highlight</item>
        <item name="android:thumbTint">@color/primary_green</item>
        <item name="android:trackTint">@color/surface_highlight</item>
    </style>
</resources>